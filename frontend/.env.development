# Used only by `npm run dev` / `vite`. Relative `/api` is proxied to Flask.
# Uncomment only if you need an absolute backend URL instead of the Vite proxy.
# VITE_API_BASE_URL=http://localhost:5000/api
